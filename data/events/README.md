# Events

Machine-readable events are appended to `events.jsonl` during monitoring runs.
Each event includes `event_id`, `source_id`, `timestamp`, `event_type`, `old_value`, `new_value`, `evidence`, `confidence`, and `review_required`.
