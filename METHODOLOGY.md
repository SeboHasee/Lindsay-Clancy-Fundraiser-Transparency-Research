# Methodology

Pipeline stages: raw -> normalized -> validated -> verified -> analytical/public.

The system preserves raw observed values and provenance before normalization, and blocks unverified assertions from being published as facts.

