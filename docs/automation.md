# Automation

The automation loop is implemented through `python -m research monitor`, `validate`, `analyze`, `report`, and `export --public`.

Scheduled workflows run these steps in order with validation and privacy checks before publication artifacts are updated.
